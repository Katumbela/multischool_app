
import 'package:flutter/material.dart';

class MovimentosTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Movimentos Content'),
    );
  }
}