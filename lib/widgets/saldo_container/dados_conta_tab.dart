import 'package:flutter/material.dart';

class DadosContaTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Dados da Conta Content'),
    );
  }
}
